services:
  app:
    restart: always
    environment:
      - APP_ENV=production
      - PORT=3000
    ports:
      - "80:3000"

  prometheus:
    image: prom/prometheus
    volumes:
      - ./prometheus.yml:/etc/prometheus/prometheus.yml
    ports:
      - "9090:9090"